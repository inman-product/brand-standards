---
export interface Props {
  cols?: 2 | 3 | 4;
  style?: string;
}

const { cols = 2, style } = Astro.props;
---

<div
  class:list={['logo-grid', { 'logo-grid-3': cols === 3 }]}
  style={style}
>
  <slot />
</div>
